# reqsync visual assets

Drop your GitHub README visuals here.

Recommended files:

- `reqsync-quick-sync.webp` (8-15s): `reqsync run --no-upgrade --dry-run --show-diff`
- `reqsync-apply-mode.webp` (8-15s): `reqsync run --show-diff`
- `reqsync-check-mode.webp` (5-10s): `reqsync run --check --no-upgrade`

Suggested capture settings:

- Width: 1280px (or 1080px)
- FPS: 12-15
- Keep text zoomed and readable
- Trim dead time and long installs

README references are already prepared for these filenames.
